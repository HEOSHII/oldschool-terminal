export enum COMMANDS {
	HELP = 'help',
	CHAT = 'chat',
	INFO = 'info',
	THEME = 'theme',
	SOUND = 'sound',
	CLEAR = 'clear',
	EXIT = 'exit',
}

export const COMMANDS_LIST: string[] = Object.values(COMMANDS);
